version: '3.8'

services:
  rdp-evaluator:
    build: .
    ports:
      - "8501:8501"  # Streamlit web interface
      - "5000:5000"  # Flask API
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1